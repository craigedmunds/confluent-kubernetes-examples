apiVersion: platform.confluent.io/v1beta1
kind: Connect
metadata:
  name: connect
  namespace: confluent
spec:
  replicas: 1
  image:
    application: cnfldemos/cp-server-connect-datagen:0.6.4-7.6.0
    init: confluentinc/confluent-init-container:2.11.1
  dependencies:
    kafka:
      bootstrapEndpoint: kafka:9071